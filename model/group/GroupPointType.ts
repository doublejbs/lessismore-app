// 그룹 지도 포인트 유형 (GRP-9, DM-29 `groups/{groupId}/points/{pointId}.type`).
enum GroupPointType {
  Water = 'water',
  Shelter = 'shelter',
  Caution = 'caution',
  Note = 'note',
}

export default GroupPointType;
