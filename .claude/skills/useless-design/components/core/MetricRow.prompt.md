창고·배낭·홈 미리보기가 공유하는 목록 행 문법.

```jsx
<Card tone="paper" padding={0}>
  <MetricRow brand="니모" name="텐서 인슐레이티드" meta="Grey · 사용률 91%" value={480} />
</Card>
```

- 값이 없는 줄은 렌더하지 않는다 — 빈 텍스트를 두면 행에 죽은 공백이 생긴다.
- 여러 행은 하나의 `Card` 안에 넣고 `.5px` 헤어라인으로 나눈다(카드를 행마다 쪼개지 않는다).
