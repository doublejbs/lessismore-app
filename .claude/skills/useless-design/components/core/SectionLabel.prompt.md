섹션을 여는 작은 대문자 라벨. 큰 제목 대신 이걸 쓰면 화면이 조용해진다.

```jsx
<SectionLabel trailing={<a href="#">전체 보기</a>}>Warehouse — 42</SectionLabel>
```

- 영문/숫자는 그대로, 한글은 대문자 변환이 없으므로 짧게 쓴다(`여행 예정`).
- 개수는 `— 42`처럼 em dash로 붙인다.
