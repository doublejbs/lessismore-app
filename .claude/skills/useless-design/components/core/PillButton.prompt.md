하단 고정 CTA와 플로팅 액션에 쓰는 알약 버튼.

```jsx
<PillButton label="패킹 시작하기" variant="accent" block />
<PillButton label="장비 추가" variant="primary" />
```

- 화면당 주 액션은 하나. `accent`(라임)는 그 하나에만 쓰고, 나머지는 `primary`.
- 라임 면 위 글자는 반드시 잉크(`--lime-on`) — 흰 글자는 대비가 안 나온다.
