# 배낭 필름 카드 공유 (SNS 이미지)

| 항목 | 내용 |
| --- | --- |
| 상태 | **구현 완료 · UX/디자인 리뷰 대기** (2026-07-25). 스펙 컴플라이언스·코드 퀄리티 리뷰는 통과. 실행 중인 앱 스크린샷 기반 UX 리뷰까지 마치면 `[제안]` 라벨을 제거하고 as-built로 전환한다([README.md](README.md) 5절) |
| ID 프리픽스 | `BS` |
| 주요 코드 | `app/bag-film-card.tsx`, `components/bag-film-card/`, `model/bag-film-card/`, `components/bag-detail/BagFilmCardButtonView.tsx` |
| 관련 스펙 | [BagDetail.md](BagDetail.md), [HealthActivity.md](HealthActivity.md), [BagDestination.md](BagDestination.md), [DataModel.md](DataModel.md), [Analytics.md](Analytics.md) |

## 1. 개요

다녀온 배낭 여행의 **사진 위에 그 여행의 기록(무게·거리·속도·날짜)을 얹어 한 장의 필름(폴라로이드) 카드**로 만들어 SNS에 공유한다. UL 백패킹의 핵심 서사인 **"이 무게로 여기까지 다녀왔다"** 를 이미지 한 장으로 압축하는 것이 목적이다.

기존 공유(BD-7)는 **링크**를 내보내지만, 이 기능은 **이미지**를 내보낸다. 둘은 헤더의 **서로 다른 아이콘**으로 갈라지는 별개의 진입점이다(BS-1).

> **폐기 이력 — "레디샷"**: 과거 배낭 이미지 공유 기능이 있었으나 진입점이 두 곳(패킹 완료·배낭 상세)으로 갈렸다가 **양쪽 다 제거되어 도달 불가 죽은 코드**가 되었고, 통째로 삭제됐다(`1e5d290`, 14파일 4,439줄). 그 교훈으로 이 기능은 **진입점을 하나로 고정**한다(BS-1). 캡처 기술(`react-native-view-shot`)은 당시 검증된 것을 재사용한다.

## 2. 화면 및 진입

```
bag/[id] (배낭 상세)
  └─ 헤더 우측 상단 `필름 카드` 아이콘 → /bag-film-card   ← 이 스펙
       (기존 우측 아이콘: 복사(BD-8) · 공유(BD-7) 와 나란히)

/bag-film-card → BagFilmCardWrapper → BagFilmCardView
                    ├─ 사진 선택 (expo-image-picker)
                    ├─ 필름 카드 프리뷰 (캡처 대상)
                    └─ 하단 CTA: 공유하기 / 저장
```

## 3. 요구사항

### BS-1 진입점 `[제안]`

사용자는 배낭 상세 헤더의 필름 카드 아이콘으로 이미지 카드 만들기에 들어갈 수 있다.

**수용 기준**

- 진입점은 **배낭 상세 헤더 우측 상단의 `필름 카드` 아이콘 버튼 하나뿐이다.** 다른 화면(패킹 완료·공유 시트 등)에 중복 진입점을 두지 않는다 — 레디샷이 죽은 원인이므로 이 규칙을 깨지 않는다.
- 기존 우측 아이콘(복사 BD-8 · 공유 BD-7)과 같은 행에 놓는다. 아이콘 전용 컨트롤이므로 `accessibilityLabel`(`필름 카드 만들기`) + `accessibilityRole='button'`을 주고 44pt 히트 영역을 확보한다.
- 아이콘이 3개가 되므로 **간격·정렬은 기존 두 아이콘과 동일한 규칙**을 따른다(iOS는 네이티브 헤더 우측 아이템, LG 규칙 준수).
- 링크 공유(BD-7)는 지금 동작 그대로 둔다 — 이 기능은 링크 공유와 별개의 진입점이다.
- **웹에서는 이 아이콘을 렌더하지 않는다** — 캡처·네이티브 공유 시트가 없다.
- 진입 시 배낭 id를 넘겨 카드 화면이 `BagDetail`을 구성한다(핸드오프 또는 라우트 파라미터).

### BS-2 사진 선택 `[제안]`

사용자는 카드에 넣을 사진을 자기 갤러리에서 고른다.

**수용 기준**

- 배낭에는 대표 사진 필드가 없다(DataModel). 사진은 **매번 사용자가 갤러리에서 고른다**(`expo-image-picker`, 권한 요청은 선택 시점).
- 카드의 사진 영역은 **정사각(1:1)** 이며, 고른 사진을 중앙 기준으로 채워 크롭한다(`cover`).
- 화면 진입 시 사진이 없으면 사진 영역에 **`사진 고르기` 플레이스홀더**를 표시하고, 탭하면 피커를 연다. 사진을 고른 뒤에도 사진 영역을 탭하면 다시 고를 수 있다.
- **사진 없이도 공유할 수 있다** — 사진 미선택 시 단색(브랜드 톤) 배경으로 카드가 완성된다. 사진 선택을 강제하지 않는다.
- 권한 거부 시 안내 후 화면을 유지한다(카드 자체는 단색 배경으로 계속 만들 수 있다).

### BS-3 카드 레이아웃 (필름/폴라로이드) `[제안]`

**수용 기준**

- 흰 필름 프레임 안에 **정사각 사진**, 그 아래 **캡션 영역**이 있는 폴라로이드 형태. 좌우 여백은 같고 **아래 여백이 더 넓다**(폴라로이드 고유 비율, 대략 4:5).
- 캡션은 **좌·우 2열, 각 열 2행**(큰 값 + 작은 값) 구조다.
- **내보내기 캔버스는 3종이며 화면에서 사용자가 고른다**(기본값 `폴라로이드 단독`). 프리뷰는 선택 즉시 바뀌고, 캡처 대상은 선택된 캔버스 전체다.

  | 옵션 | 캔버스 | 구성 |
  | --- | --- | --- |
  | `카드`(기본) | 폴라로이드 그 자체 (대략 4:5) | 배경 없음 |
  | `4:5` | 검은 배경 4:5 | 폴라로이드를 캔버스 폭의 **66%** 로 가운데 배치(사방 여백 균일 17%) |
  | `9:16` | 검은 배경 9:16 | 폴라로이드를 캔버스 폭의 **68%** 로 가운데 배치(좌우 16% · 상하 26.1%) |

  - 비율 선택 컨트롤은 앱 공용 `CategoryChipView`를 쓴다(카드 자체가 아니라 **앱 UI**라 디자인 토큰을 따른다).
  - 캡처·공유가 진행 중일 때는 비율을 바꿀 수 없다(화면과 결과물이 어긋나지 않게).
- **배경 캔버스가 있는 `4:5`·`9:16`에서만 폴라로이드에 그림자를 넣어 배경 위에 떠 보이게 한다.** `카드`(배경 없음)에는 넣지 않는다 — 잘리거나 이상한 테두리로 남는다.
  - 순수 검정 배경에서는 어두운 그림자가 보이지 않으므로, **옅게 확산하는 밝은 광휘**(가장자리 약 12% → 바깥으로 소멸)로 구현한다. 과하면 싸구려 합성처럼 보이므로 은은한 수준을 유지한다.
  - 그림자는 **네이티브 그림자 속성(`elevation`)이 아니라 실제로 그려지는 View 겹침**으로 만든다 — Android 캡처는 소프트웨어 캔버스라 `elevation` 그림자가 내보낸 이미지에서 사라진다.
  - 확산 폭은 **캔버스 여백을 넘지 않아야 한다**(넘으면 캡처 경계에서 잘린다). 캡처 대상 캔버스와 그 사이 컨테이너에는 `overflow: 'hidden'`을 걸지 않는다.
- 사진과 프레임 사이에 **아주 옅은 안쪽 경계선**을 두어 실물 인화지 느낌을 준다(밝은 사진이 프레임에 뭉개지는 것도 막는다).
- **프레임 바탕은 인화지 텍스처 이미지**(`assets/images/film-frame-paper.jpg`, 1080×1352)를 카드 배경에 깔아 표현한다. 그레인·섬유질 얼룩·가장자리 음영·광택이 이미지에 구워져 있다.
  - 이 텍스처는 **직접 생성한 에셋**이다. 시판/무료 배포 폴라로이드 프레임 이미지는 상표(트레이드 드레스)·라이선스 문제가 있고, 종이 그레인은 RN 코드만으로는 만들 수 없어(성능·밴딩·해상도 문제) 이미지로 해결했다. 상세 근거는 8절 참고.
  - **사진 창이 뚫린 프레임이 아니라 종이 전체 텍스처**다. 따라서 사진·캡션 위치를 텍스처 좌표에 맞춰 하드코딩할 필요가 없다.
  - 텍스처 비율(1.2519)과 카드 비율(약 1.2505)이 사실상 같아 왜곡 없이 `cover`로 깐다.
  - 텍스처는 알파가 없는 사각 이미지라 **모서리 라운딩은 RN이 잘라준다**(카드에 작은 `borderRadius` + `overflow: 'hidden'`). 라운딩은 폭 대비 약 0.7%로, 과하면 스티커처럼 보인다.
  - 질감은 **프레임에만** 적용한다 — **사진은 원본 그대로** 둔다(필름 색감 필터는 v1 범위 밖, 8절 참고).
  - 텍스처는 일반 이미지라 양 플랫폼 캡처에 그대로 찍힌다. 코드로 덧그리던 오프화이트 배경색·광택 그라디언트는 텍스처와 중복되므로 **쓰지 않는다**.
- 캡션 텍스트는 **날려쓴 손글씨(필기체)** 로 렌더한다.
  - 영문·숫자(날짜·`KG`·`KM`·`KM/H`): **Permanent Marker**. 레퍼런스 이미지와 후보 폰트를 렌더해 대조한 결과 굵기·기울기·획 끝 처리가 가장 근접해 선정했다. 얇은 필기체는 사진 위에서 힘이 약하다.
  - **캡션은 영문·숫자 전용이며 폰트도 이 하나만 쓴다.** 한글 손글씨체는 마커체와 굵기·톤이 맞지 않아 섞지 않는다(8절 참고).
  - 크기는 폰트 실측 지표로 맞춘다. 캡션에는 소문자가 없고 숫자·대문자뿐이라 x-height가 아니라 **cap-height/em**을 기준으로 한다.
  - 기존 `Gaegu`·`Inter`·`Nothing You Could Do`·`Caveat`는 이 카드에 쓰지 않는다.
- **캡션에는 한글을 쓰지 않는다** — 값과 단위(`KG` · `KM` · `KM/H`)만 얹는다. `무게`·`거리` 같은 한글 라벨도, 장소명 같은 한글 데이터도 넣지 않는다.
- 카드는 앱 디자인 토큰이 아니라 **내보내기 캔버스 팔레트**를 따른다(인화지 텍스처·검정 텍스트, 배경 캔버스는 검정). `CLAUDE.md`의 "공유 이미지 내보내기 캔버스" 토큰 예외 조항에 해당한다.

### BS-4 카드에 얹는 데이터 `[제안]`

**수용 기준**

- **운동 기록이 연결된 배낭**(HA-3)은 4개 값을 얹는다.

  | 위치 | 값 | 형식 | 출처 |
  | --- | --- | --- | --- |
  | 좌·큰 | 날짜 | `26.07.26` (YY.MM.DD) | `BagDetail.getStartDate()` |
  | 좌·작 | 배낭 총 무게 | `7.2 KG` | `BagDetail.getWeight()` |
  | 우·큰 | 이동 거리 | `12.4 KM` | `BagActivitySummary.distance`(m) |
  | 우·작 | 평균 속도 | `3.2 KM/H` | `distance / duration` 파생 |

- **운동 기록이 없는 배낭**은 **우측 열을 비운다** — 좌측 날짜·무게만 있는 카드도 유효하다. 장소명 등 다른 값으로 대체하지 않는다(캡션이 영문·숫자 전용이라 한글 장소명을 넣지 않는다, BS-3).

  | 위치 | 값 | 형식 | 출처 |
  | --- | --- | --- | --- |
  | 좌·큰 | 날짜 | `26.07.26` | `BagDetail.getStartDate()` |
  | 좌·작 | 배낭 총 무게 | `7.2 KG` | `BagDetail.getWeight()` |
  | 우 | (비움) | — | — |

- **날짜는 날짜만** 표시한다 — 시각(`12:08 PM`)은 넣지 않는다.
- **속도는 페이스(`분'초"/km`)가 아니라 시속(`KM/H`)** 으로 표기한다. 소수 1자리.
- 거리는 소수 1자리(`12.4 KM`), 무게는 소수 1자리(`7.2 KG`).
- 값은 **표시 시점에 이미 로드된 배낭·요약 데이터**로 구성한다. 이 화면 때문에 건강 허브를 새로 조회하지 않는다(HA-5의 저장 요약값 사용).

### BS-5 공유·저장 `[제안]`

**수용 기준**

- 카드 프리뷰를 **`react-native-view-shot`의 `captureRef`** 로 캡처해 이미지 파일을 만든다(과거 레디샷과 동일 경로). Skia는 도입하지 않는다.
- 캡처 해상도는 SNS 업로드에 충분하도록 **가로 1080px 기준**으로 맞춘다.
- **공유하기**: iOS는 `Share.share({ url })`, Android는 `Sharing.shareAsync(uri)`(`expo-sharing`)로 OS 공유 시트를 연다 — 인스타그램 등 앱 선택은 시스템에 맡긴다.
- **저장**: `expo-media-library`로 갤러리에 저장하고 토스트로 알린다(`저장했어요`). 권한 거부 시 안내한다.
- 캡처·공유 중에는 CTA를 로딩 상태로 두어 중복 실행을 막는다. 실패 시 토스트로 알리고 화면을 유지한다.

### BS-6 애널리틱스 `[제안]`

[Analytics.md](Analytics.md) AN-3 표에 아래를 추가한다.

| 이벤트 | 시점 | 파라미터 |
| --- | --- | --- |
| `click_film_card_open` | 배낭 상세 헤더 `필름 카드` 아이콘 진입 (BS-1) | — |
| `click_film_card_photo` | 사진 선택 완료 (BS-2) | — |
| `click_film_card_share` | 공유하기 (BS-5) | `has_activity`: boolean, `has_photo`: boolean |
| `click_film_card_save` | 갤러리 저장 (BS-5) | — |

## 4. 데이터

- **새로 저장하는 데이터가 없다.** 카드는 기존 배낭 데이터(BD-3 무게, DM-14 날짜, DM-22 운동 요약, 여행지 스냅샷)를 읽어 그리기만 한다. 고른 사진도 저장하지 않는다(캡처 후 파기).
- 따라서 [DataModel.md](DataModel.md)에 추가되는 필드가 없다.

## 5. 플랫폼 분기

| 지점 | iOS | Android | Web |
| --- | --- | --- | --- |
| 진입점(헤더 `필름 카드` 아이콘) | 노출 | 노출 | **숨김** |
| 공유 | `Share.share({url})` | `Sharing.shareAsync(uri)` | — |
| 저장 | `expo-media-library` | `expo-media-library` | — |

## 6. 엣지 케이스

- **비로그인**: 배낭 상세 자체가 로그인 전용이라 이 화면에 도달하지 않는다.
- **사진 미선택**: 단색 배경으로 카드 완성(BS-2).
- **운동 기록 없음**: 우측 열 비움 — 좌측 날짜·무게만 있는 카드(BS-4).
- **권한 거부**(사진·갤러리): 안내 후 화면 유지, 카드는 계속 만들 수 있다.
- **캡처 실패**: 토스트 안내, 화면 유지.

## 7. 수동 검증 체크리스트

- [ ] 배낭 상세 헤더 `필름 카드` 아이콘으로 진입 (다른 진입점 없음)
- [ ] 사진 고르기 → 정사각 크롭으로 카드에 반영, 다시 고르기 동작
- [ ] 사진 없이도 카드 완성·공유 가능
- [ ] 운동 기록 있는 배낭: 날짜/무게 · 거리/속도 4칸 표시, 날짜는 `26.07.26` 형식, 속도는 `KM/H`
- [ ] 운동 기록 없는 배낭: 우측 열이 비고 좌측(날짜·무게)만 남아도 어색하지 않음
- [ ] 비율 칩 `카드`/`4:5`/`9:16` 전환 시 프리뷰가 즉시 바뀌고, 4:5·9:16은 검은 배경 가운데에 폴라로이드가 놓임
- [ ] 4:5·9:16에서 폴라로이드가 배경에서 떠 보이고, **저장/공유한 이미지에도 그림자가 잘리지 않고 들어감**(iOS·Android 각각 확인)
- [ ] `카드` 옵션에는 그림자가 없음
- [ ] 세 비율 모두 캡처 결과가 가로 1080px이고 선택한 비율과 일치
- [ ] 캡션에 한글이 전혀 없다(라벨·장소명 모두 — 값과 단위 `KG`·`KM`·`KM/H`만)
- [ ] 캡션 영문·숫자가 `Permanent Marker`로 렌더됨
- [ ] 폴라로이드 바탕에 인화지 텍스처(그레인·얼룩·가장자리 음영)가 보이고, 저장/공유한 이미지에도 동일하게 들어감
- [ ] 4:5·9:16에서 폴라로이드가 캔버스 대비 충분히 작아 사방 여백이 넉넉함
- [ ] 공유 시트로 이미지 전달, 갤러리 저장 동작
- [ ] 웹에서 헤더 `필름 카드` 아이콘이 보이지 않음

## 8. 미해결 질문

- 폰트는 **영문 `Permanent Marker` 하나로 확정**했다(`@expo-google-fonts/permanent-marker`). 초안의 `Nothing You Could Do`(너무 가늚) → `Caveat 700`(레퍼런스와 인상 불일치) → `Permanent Marker` 순으로 교체했고, 쓰지 않게 된 패키지는 모두 제거했다.
- **한글은 카드에서 뺐다**(2026-07-25). 유일한 한글 값이던 장소명에 `Nanum Brush Script`(400 브러시체)를 썼으나, 마커체 영문과 굵기·톤이 끝내 맞지 않아 **장소명 자체를 카드에서 제거**하고 캡션을 영문·숫자 전용으로 정리했다(BS-3/BS-4). `@expo-google-fonts/nanum-brush-script`와 장소명 자동 축소 로직도 함께 삭제했다. 카드에 장소를 다시 넣으려면 마커체와 어울리는 굵은 한글 손글씨체를 먼저 찾아야 한다.
- 이 카드에 쓰지 않는 `Gaegu`·`Playwrite NZ`의 정리 여부는 별도 판단.
- `expo-linear-gradient`는 코드 기반 광택을 걷어내면서 이 저장소에서 참조가 0건이 됐다(이 기능 도입 전부터 있던 의존성). 제거 여부는 별도 판단.
- 사진에 필름 특유의 색감(그레인·바랜 톤) 필터를 넣을지 — v1은 원본 그대로.
- **프레임 질감은 직접 생성한 인화지 텍스처 이미지로 해결했다**(2026-07-25, `assets/images/film-frame-paper.jpg`). 처음엔 코드(오프화이트 + 광택 그라디언트 + 라운딩)로만 표현했으나 "전혀 느낌이 안 산다"는 평가를 받아 텍스처로 전환했다. 결정 이력과 근거:
  - **시판/무료 프레임 에셋은 쓰지 않는다.** Polaroid사가 흰 테두리 형태(classic border logo) 트레이드 드레스를 적극 방어 중이고(Fujifilm Instax 상대 소송 진행), 무료 PNG 집합 사이트는 출처·재배포 조건이 불투명해 App Store 배포 앱에 부적합하다. → **직접 생성**해 이 문제를 피했다.
  - **종이 그레인은 RN 코드만으로는 만들 수 없다.** (1) View를 수백 개 겹치면 성능·캡처 비용이 크고, (2) 스트라이프·도트는 밴딩이 도드라져 오히려 싸구려로 보이며, (3) data URI 노이즈 타일은 `resizeMode='repeat'`가 dp 단위로 타일링돼 고배율 화면에서 뭉개진다. 픽셀 단위 그레인은 이미지가 유일한 현실적 수단이다.
  - **사진 창을 뚫지 않은 전면 텍스처**라, 프레임 에셋 방식의 고질적 단점(사진·캡션 좌표 하드코딩)을 겪지 않는다.
  - **해상도**는 가로 1080px 캡처에 맞춰 1080×1352로 만들었다(159KB). 폴라로이드가 캔버스 폭의 66~68%로 축소 배치되므로 실제 필요 해상도는 이보다 낮아 여유가 있다.
- 여러 장 사진(콜라주)은 범위 밖. 필요해지면 별도 요구사항으로 다룬다.
