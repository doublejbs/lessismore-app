# 장비 상세

| 항목 | 내용 |
| --- | --- |
| 상태 | as-built (2026-07-11 코드 기준) · **2026-07-28 개정(as-built)**: 장비 이미지 미제공([DataModel.md](DataModel.md) §1) — GD-4 폐기, GD-1 텍스트 우선 레이아웃 · **2026-08-11 개정(as-built)**: Liquid Depth 이식 — 시각 서술(타이포·면·모서리·섹션 제목·하단 CTA)을 목업 §9 기준으로 정정, 로딩 스켈레톤 도입 · **같은 날 디자인 리뷰 반영(as-built)**: `사진 추가` 점선 슬롯을 `수정` 폼으로 이동하고 상세는 사진이 있을 때만 그린다(GD-1·GD-13 · [GearEdit.md](GearEdit.md) GE-2), 무게를 이름 아래 독립 줄로(GD-1), 라임 면은 값이 1 이상일 때만(GD-9), 여행 카드 주소를 시·군·구까지 축약(GD-10) |
| ID 프리픽스 | `GD` |
| 주요 코드 | `app/gear-detail/[id]/`, `components/warehouse-detail/`, `model/warehouse-detail/WarehouseDetail.ts` |
| 관련 스펙 | [DataModel.md](DataModel.md), [Reply.md](Reply.md), [Warehouse.md](Warehouse.md), [Search.md](Search.md), [GearEdit.md](GearEdit.md), [LiquidGlassNavigation.md](LiquidGlassNavigation.md) |

## 0. 디자인 세대 (2026-08-11)

이 화면은 **Liquid Depth**로 이식됐다(디자인 정본 `.claude/skills/useless-design/`, 목업 §9). 시각 값의 단일
소스는 `constants/DesignTokens.ts`의 `Liquid*` 그룹이며, 아래 요구사항의 수치는 그 토큰 이름으로 읽는다.

- 지면은 **지형 없는 `canvas` + 좌측 중단 라임 글로우**(`LiquidBackdrop screen='none' glowPosition='leftMid'`) —
  읽을 지형이 아니라 장비 하나를 판단하는 화면이다.
- 크롬은 지면 위에 뜬 **유리**다: iOS는 네이티브 투명 헤더(LG-1), Android·Web은 공용 `LiquidHeaderChrome`
  (유리 원형 back + 유리 캡슐 `[공유][수정]`)가 같은 그림을 그린다.
- 섹션은 큰 제목이 아니라 **대문자 마이크로 라벨**(`LiquidSectionLabel`)로 연다. 내용은 흰 카드 하나에 담고
  행은 헤어라인으로만 가른다 — 행마다 면을 두지 않는다.
- **화면의 라임 면은 하나**다: 사용 지표 첫 타일(GD-9). 주 액션도 하나 — 하단 고정 잉크 CTA(GD-14).

## 1. 개요

장비 1개의 상세 화면. 기본 정보, 이 장비를 담았던 배낭 기록(사용/미사용 통계), 리뷰(댓글) 미리보기를
제공한다. 창고·검색·배낭 상세 어디서든 `/gear-detail/{id}`로 진입한다.

## 2. 화면 및 진입

```
app/gear-detail/[id]/index.tsx → WarehouseDetailWrapper → WarehouseDetailView
  ├─ WarehouseDetailInformationView   (사진[있을 때만]/제조사/이름/무게/태그 칩)
  ├─ WarehouseDetailBagRecordView     (배낭 기록 — bags가 있을 때만)
  └─ WarehouseDetailReviewSectionView (리뷰 미리보기)
```

- 장비 조회는 `GearStore.getGear(id)`: `users/{uid}/gears/{id}` 우선, 없으면 카탈로그 `gear/{id}`.

## 3. 요구사항

### GD-1 기본 정보 섹션

**수용 기준**

- 제조사(`getDisplayCompany()` — 한글 표시명 우선, 리스트 화면과 동일 — [Warehouse.md](Warehouse.md) WH-1 공통 규칙), 이름(`getDisplayName()`), 색상, 무게를 표시한다. **이미지는 사용자가 올린 본인 사진만 표시한다**([DataModel.md](DataModel.md) §1, 2026-07-29 개정) — 카탈로그 크롤 이미지는 계속 쓰지 않으며 **폴백하지 않는다**. 업로드·교체·삭제는 GD-13.
  - 구성은 **사진 줄 + 정체 줄** 두 층이다(2026-07-29 사용자 결정).
    - **1층 — 사진**: **140pt 정사각**(radius `LiquidRadius.card`)을 **가운데 단독 줄**로 둔다. 사진이 없으면 **이 줄 자체를 렌더하지 않는다** — 빈 칸·플레이스홀더를 남기지 않는다(이미지 없는 장비가 다수라 그쪽이 기본 모습이다).
      - **상세의 사진은 보여주기만 한다**(2026-08-11 디자인 리뷰). 추가·교체·삭제는 `수정` 폼의 사진 필드가 맡고(GD-13), 상세는 `Gear.getImageUrl()`을 그리는 것 하나만 한다 — 화면 복귀 시 전체 재조회(GD-9) 결과가 그대로 반영되고, 진입 시점 URL로 씨를 받는 업로드 모델과 값이 어긋나지 않는다.
    - **2층 — 정체 줄**: 브랜드(13/600 `inkMuted`) → 이름(`LiquidType.title2` 28/36) → **무게** → **태그 칩 줄** 순으로 쌓는다(2026-08-11 디자인 리뷰). 사진 유무와 **무관하게 항상 이 형태**다.
      - 브랜드는 이름과 **같은 크기로 두지 않는다**(ACG 시절 24/24) — 이름이 28pt 화면 대상이 되면서 브랜드는 그 이름을 여는 라벨 자리로 내려왔다.
      - **[이력] 2026-08-11(이식 직후)**: 무게가 태그 칩 줄과 같은 행의 우측에 아랫선을 맞춰(`flex-end`) 놓였다. 실측에서 칩보다 아래 베이스라인에 앉아 **칩 줄에도 속하지 않고 아래 블록에 붙어 보였고**, 단위 `g`를 18pt로 작게 앉히니 아래첨자처럼 매달렸다 — 화면의 핵심 수치인데 소속이 없었다.
  - **좌 정체 · 우 지표는 앱 전역의 행 문법이다** — 창고 목록([Warehouse.md](Warehouse.md) WH-1)·배낭 목록([Bag.md](Bag.md) BAG-1)·배낭 상세 장비 행이 모두 이 문법이고 무게가 항상 우측 같은 자리에 온다. 상세만 다른 문법을 쓰면 목록에서 익힌 자리 감각이 끊긴다.
  - **무게는 이름 바로 아래 좌측 독립 줄이고, `LiquidType.numXl`(Archivo Narrow 42/38) 한 스팬이며 `무게` 캡션은 생략**한다(2026-08-11 디자인 리뷰). 값과 단위는 **한 덩어리**(`1510g` — `formatGearWeight`가 그대로 내주는 문자열)로 **같은 크기·같은 베이스라인**에 쓴다 — 단위를 작게 떼어 앉히면 아래첨자처럼 매달려 수치가 불안정해 보인다. 무게가 미입력이면 줄째로 뺀다. 표기는 [DataModel.md](DataModel.md) DM-26을 따른다(장비 개별 무게는 항상 g, 천 단위 구분자 없음). `g`가 이미 무엇인지 말해주고 목록 행에도 캡션이 없다. 캡션 없이도 화면에서 가장 큰 숫자라 **시각 앵커는 무게**다 — 사진은 "내 물건이 맞나" 확인용이지 판단 근거가 아니며, 앱 정체성이 무게 줄이기다. 콘덴스드 서체는 한글 글리프가 없어 **숫자·단위에만** 쓴다.
  - **사진 유무로 무게 배치를 바꾸지 않는다.** 사진이 있을 때만 무게를 컬럼 안에 넣고 없을 때는 히어로로 키우면 같은 화면이 두 얼굴이 된다.
  - 폐기된 시안(기록): ① 사진을 위에 전체 폭으로 두기 — 393pt 폭에서 140pt만 쓰고 우측 250pt 이상이 빈 채로 남았다. ② 사진을 좌측 100pt로 줄이고 우측 컬럼에 정보를 몰기 — 우측 여백은 해소됐지만 사진이 작아지고, 무게를 컬럼 안에 넣으니 목록 행과 자리 감각이 어긋났다.
- 카테고리·색상·사이즈는 무게 줄 아래 **태그 칩 줄**로 표시한다(2026-08-11 개정 — 목업 §9). 색상이 여기 있는 것은 창고 목록 서브라인에서 내려온 결과다([Warehouse.md](Warehouse.md) WH-1) — 목록에서 할 일을 만들지 않는 값이라 판단하는 화면에서만 말한다. 값이 있는 항목만 `LiquidChip size='sm'`(h28, 알약, `chipFill` + 0.5px `chipStroke`, `inkSecondary`) 하나씩 두고, 줄이 넘치면 다음 줄로 흐른다. 카테고리는 **세분 카테고리 한글 라벨**(DM-4, 매핑에 없으면 그룹 라벨 폴백), 색상은 `colorKorean || color`, 사이즈는 `sizeKorean || size`. 빈 항목은 생략, 모두 없으면 칩 줄을 렌더하지 않는다.
  - **[이력] 2026-07~08-10**: `카테고리 · 색상 · 사이즈`를 이어 붙인 회색 **메타 라인** 한 줄이었다(칩 아님). 이름이 28pt 화면 대상으로 커지면서 그 아래 회색 한 줄이 이름의 부제처럼 읽혀 칩으로 갈랐다 — 세 값은 서로 다른 축의 사실이라 한 문장으로 이어 붙일 이유가 없다.
  - 태그는 **누를 수 없다** — 표시용이라 `LiquidChip`에 `onPress`를 주지 않고, 이때 프리미티브는 `button` 롤 없는 `View`로 떨어진다(못할 동작을 스크린리더에 약속하지 않는다).
  - 글자·여백은 목업(12.5 · 좌우 12)이 아니라 **`LiquidChip size='sm'`의 값(13.5 · 좌우 15)** 그대로다 — 같은 알약을 화면마다 다른 값으로 그리지 않도록 **프리미티브가 목업 수치보다 우선**한다.
- 태그 칩 줄은 정체의 **마지막 층**이다(이름 → 무게를 읽은 뒤 확인하는 사실들). 태그가 하나도 없으면 줄째로 빼 죽은 여백을 남기지 않는다.
- `수정하기`는 본문 중앙 텍스트 버튼 대신 **헤더 우상단** 액션으로 둔다(보유 장비만, 라벨은 `수정`). 화면 본문의 중앙 정렬 액션 나열을 줄여 주 액션 위계를 명확히 한다(HIG).
- 헤더: 스크롤이 정체 블록을 지나면 헤더에 **제품명 타이틀**이 나타난다(컨텍스트 유지). **최상단에서는 비운다** — 본문 이름(28/36)이 화면 대상이라 바에 같은 말을 또 두면 중복이고, 목업 §9 헤더에도 타이틀이 없다. iOS는 `headerTitle`을 스크롤 상태에 따라 채우고(2026-08-11 — 그전에는 상시 표시), Android·Web은 유리 크롬 가운데 자리에 같은 규칙으로 넣는다.
- 접근성·터치 타깃(HIG): 뒤로가기 버튼은 44×44pt 터치 영역과 `accessibilityLabel`/`accessibilityRole`을 가진다. 헤더 액션도 동일 기준 — 단 Android·Web 유리 캡슐 안 칸은 34×38이 상한이고 그 38은 `hitSlop`(세로 ±2, 가로 0)으로 채운다(`LiquidGlassCapsule`의 클리핑 제약, LG-3·창고 헤더와 같은 자리·같은 값).
- 두 액션이 **다 빠지는 장비**(커스텀이고 미보유 — 공유도 수정도 없음)에서는 액션 자리를 **아예 비운다**. Android·Web은 유리 캡슐 자체를 그리지 않는다 — 누를 것 없는 빈 알약이 헤더에 떠 있으면 안 된다.

### GD-2 배낭 기록 섹션

사용자는 이 장비를 어떤 배낭에 담았고 실제로 썼는지 볼 수 있다.

> GD-9~GD-12(사용 인사이트)로 **대체·확장됨**. 아래 기준은 확장 전 기본 동작으로 유지된다.

**수용 기준**

- 장비의 `bags`가 0개면 섹션 자체를 렌더하지 않는다.
- 담긴 배낭 수, USED 횟수(`used.length`), USELESS 횟수(`useless.length`)를 표시한다.
- 횟수가 0회인 지표 타일은 **값을 라벨 수준까지 낮춘다**(`LiquidStatTile dim`). 흰 면은 라벨(`inkSecondary`)보다 **한 단계 옅은 `inkMuted`**로, 라임 면은 라벨과 **같은 `limeOnQuiet`**로 내린다 — 라임 위에서 잉크 계열로 내리면 대비가 오히려 세져 낮춘 것으로 읽히지 않는다. 면은 그대로 둬 타일 줄의 리듬이 깨지지 않게 한다.
  - 타일 라벨은 목업의 `inkMuted`/500이 아니라 **`inkSecondary`/600**이다 — 12.5px에서 흰 면 대비 AA(4.5:1)에 못 미쳐 한 단계 올렸다(HIG 대비). 값이 라벨보다 옅어지는 위 순서는 그대로 유지된다.
- 배낭별 행에 사용 여부 태그(USED/USELESS)를 표시하고, 행을 누르면 해당 배낭 상세로 이동한다.

### GD-9 사용 지표 히어로 `[제안]`

> **화면 복귀 시 전체 재조회**(2026-07-31). 사용 여부 기록(`/useless/{id}`)·배낭 편집·리뷰 작성에서 돌아오면 지표도, 함께한 여행 목록도, 리뷰도 바뀌어 있을 수 있다. 무엇이 바뀌었는지 화면이 알 방법이 없어 **포커스 시 통째로 다시 읽는다**.
>
> - 재조회는 `initialized` 플래그를 건드리지 않는다 — 내리면 래퍼가 잠깐 아무것도 안 그려 화면이 깜빡인다.
> - 첫 진입은 초기화가 이미 읽으므로 중복 조회하지 않는다(초기화 전에는 재조회가 아무것도 하지 않음).

보유 장비의 상단(기본 정보 아래)에 **내 기록 요약**을 보여준다 — "이 장비를 계속 데려갈까"에 답하는 화면의 시작점. 모든 값은 이미 로드되는 데이터의 파생값이며 **추가 Firestore 읽기 0회**.

**수용 기준**

- **보유 장비(`isAdded`)에만** 표시한다. `bags`가 0개면 지표 없이 안내만 표시한다 — 조용한 면(`LiquidCard tone='quiet'`, radius `tile`)에 담아 섹션이 비어 보이지 않게 한다.
  - 안내는 **사실 + 다음 걸음 두 줄**이다(핸드오프 카피 규칙): `아직 배낭에 담은 적이 없어요` / `다음 여행 배낭에 담아볼까요?`(둘째 줄은 `LiquidType.caption` `inkSubtle`로 한 단계 옅게). 첫 줄만 두면 왜 비었는지는 알려주면서 무엇을 하면 채워지는지는 말하지 않는다.
- 지표: **사용 M** · **안 쓴 장비 K** 둘뿐이다(2026-08-11 사용자 결정 — 라벨을 `사용 안함`에서 창고 칩([Warehouse.md](Warehouse.md) WH-2-1)·내 정보 지표([Auth.md](Auth.md) AU-4)와 **같은 말**로 통일했다. 같은 뜻을 화면마다 다르게 말하고 있었다). 아래 타임라인 행의 3-상태 태그는 `사용 안함`을 그대로 쓴다 — 그건 여행 한 건의 상태이고 이 타일은 누적 지표다. 산정 기준은 **실제 로드된 배낭 목록**(삭제 잔여 id 제외) — 타임라인 행 수·"함께한 여행 N회" 헤더와 항상 일치해야 한다.
- 표현은 `LiquidStatTile` 두 장을 가로로 나란히(gap 10) 둔다. **첫 타일(`사용`)만 라임 면 + `shadow accent`이고, 이것이 화면의 유일한 라임 면**이다(2026-08-11, 목업 §9) — 형광펜 띠로 섹션 제목을 강조하던 ACG 문법은 이 면이 대신하므로 함께 걷었다.
  - **단, `사용`이 0이면 라임 면을 걷고 흰 면(`tone='paper'`) + 낮춘 숫자(`dim`)로 둔다**(2026-08-11 디자인 리뷰). 화면 유일한 최대 강조가 `0 사용`에 놓이면 "기록 없음"을 가리키면서 뭔가 달성한 것처럼 읽힌다. 라임은 **셀 것이 있을 때만** 채운다 — 값 0에 최대 강조를 주지 않는 것이 이 시스템의 규칙이고, 그 결과 이 화면에 라임 면이 하나도 없는 상태(담아만 갔고 쓴 적 없는 장비)가 정상이다.
  - `안 쓴 장비` 타일은 0이면 숫자만 낮춘다(면은 원래 흰 면) — 여기에 라임을 옮겨 붙이지 않는다. 안 쓴 장비가 많다는 신호는 덜어내기 배너(GD-12)가 맡는다.
  - **목업 §9의 세 번째 타일(`여행 11`)은 두지 않는다.** 담김 수는 아래 `함께한 여행 N회` 헤더가 같은 값을 말해 2026-07-30에 걷어낸 지표이고(아래 항목), 목업은 그 결정 전 화면을 그린 것이다. 디자인의 "타일 3개" 서술보다 이 제품 결정이 우선한다.
- ~~**담김 N회**~~ · ~~**미기록 R**~~ — **2026-07-30 제거(사용자 결정).** 담김 수는 바로 아래 `함께한 여행 N회` 헤더가 같은 값을 말해 중복이었고, 미기록은 타임라인의 `미기록` 태그로 행마다 이미 드러난다. 미기록을 "사용 안함"으로 합산하지 않는다는 3-상태 원칙(BD-5 정합)은 그대로다 — 히어로에서 세지 않을 뿐 상태 자체는 유지된다.
- ~~**사용률**(`used/(used+useless)`)~~ · ~~**보유 D일째**~~ — **2026-07-29 제거(사용자 결정).** 지표 타일(담김·사용·사용 안함·미기록) 아래 보조 문구로 두 값을 합쳐 보여줬으나, 사용률은 미기록이 분모에서 빠져 6번 담고 2번만 기록한 장비가 `사용률 100%`로 읽히는 문제가 있었다. 모수 병기(`사용률 100%(2건)`)로 정직하게는 만들었지만 문구만 길어지고 타일이 이미 같은 정보를 더 정확히 보여준다는 판단이다. `보유 D일째`도 함께 걷었다. 되살릴 경우 `createDate`가 신뢰 불가한 경우(카탈로그 진입 등) 미표시 규칙이 필요하다.
- 기존 GD-2 상단 통계 카드(담김/USED/USELESS)는 이 히어로로 통합한다(중복 표기 금지).

### GD-10 함께한 여행 타임라인 `[제안]`

GD-2의 배낭별 행을 **여행 카드**로 강화한다. 배낭 문서에 이미 있는 필드만 쓴다(추가 읽기 0회).

**수용 기준**

- 정렬: `startDate` 내림차순(최신 여행 먼저). 날짜 없는 배낭은 뒤로.
- 행 구성: 배낭 이름 · 기간(`startDate~endDate`) · **여행지 이름**(`location.name`, 없으면 생략) · **날씨 요약**(`weather.daily`에서 기간 내 최저~최고 기온 범위 + 강수 있으면 비 아이콘, 데이터 없으면 생략) · **사용 여부 3-상태 태그**(`사용` / `사용 안함` / `미기록`).
  - **태그 자리에 문장을 넣지 않는다.** 실측에서 미기록 행이 `사용 여부를 입력해주세요`라는 한 문장으로 렌더돼, 배낭 이름이 말줄임되는데 보조 문구가 더 넓은 자리를 차지했다(2026-07-29). 미기록도 **`미기록` 태그 한 단어**로 두고, 입력 유도는 행 우측 꺾쇠(›)와 탭 동작이 담당한다.
  - **`미기록`이 세 태그 중 가장 도드라진다.** `사용`·`사용 안함`은 이미 끝난 기록이라 조용해도 되지만, 미기록은 사용자가 눌러주길 바라는 유일한 상태다. 실측에서 `사용`이 검정 채움이고 `미기록`이 저대비 회색이라 **위계가 정반대**였다(2026-07-29). 완료된 두 상태를 낮추고 미기록을 한 단계 올린다.
  - 세 태그는 크기·모서리가 같고 면·글자색만 갈린다(완전한 알약, 높이 22 하한): `미기록` = `ink` 면 + `surface` 글자 / `사용` = `badgeFill` + `inkSecondary` / `사용 안함` = 면 없이 0.5px `hairlineStrong` 테두리 + `inkTertiary`.
- **행은 흰 카드 하나 안에 쌓고 헤어라인으로만 가른다**(2026-08-11 — 창고 목록 WH-1과 같은 목록 문법). 행마다 면을 두면 목록이 카드 더미로 보인다. (그전에는 행마다 각진 종이 면이었다.)
- **여행지 이름은 시·군·구까지만 남기고 뒤는 버린다**(2026-08-11 디자인 리뷰 — `model/warehouse-detail/TripLocationName.ts`). `location.name`에 전체 주소가 저장된 배낭이 있어(`강원특별자치도 강릉시 성산면 보광리 산 544`) 말줄임에만 맡기면 **단어 중간에서 잘려**(`경기도 성남시 분당구 미금일로…`) 어디였는지도 알 수 없고 잘렸다는 사실만 남았다. 자르는 자리를 글자 수가 아니라 **행정 단위**로 정한다 — 여행 기록에서 알아야 하는 건 지역이지 번지수가 아니다.
  - 토큰 중 **가장 뒤에 오는 `시`·`군`·`구`**까지 남긴다(`경기도 성남시 분당구`처럼 시와 구가 함께 오는 주소가 있다). 행정 단위가 없으면 주소가 아니라 지명(`북한산국립공원`)이라 그대로 둔다.
- **메타 라인(기간 · 여행지)은 1줄로 말줄임한다.** 위 축약 뒤에도 긴 지명은 남으므로 안전망은 유지한다 — 메타가 2줄로 흐르면 행 높이가 제각각이 되고, 주 정보인 배낭 이름보다 보조 정보가 길어져 위계가 뒤집힌다. **`[미해결]`** 저장 시점에 짧은 지명을 넣는 문제는 [BagDestination.md](BagDestination.md) 쪽 데이터 이슈로 별도 처리한다.
- 행 탭 시 배낭 상세 이동(기존 동작 유지).
- 챙김/안 챙김(패킹) 표시는 **두지 않는다** — 구현 후 사용자 결정으로 제거(2026-07-23). 패킹 기반 인사이트(챙겼는데 사용 안함 등)는 Packing.md 후속 아이디어로 남긴다.
- `BagItem` 매핑에 `weather`·`location`(name)·`activity` 필드를 추가한다(현재 매핑에서 버려지는 필드 활용 — 데이터 계약 변경 없음).
- 배낭이 30개를 초과하면 `in` 쿼리를 30개 단위로 청크 분할해 모두 조회한다([DataModel.md](DataModel.md) DM-25).

### GD-11 함께한 활동 누적 `[제안]`

**사용(`used`)한 여행**들의 운동 기록(DM-22 `bag.activity`)을 합산해 보여준다.

**수용 기준**

- 합산 대상: `used`에 포함된 배낭 중 `activity`가 있는 것. 대상이 0개면 섹션을 렌더하지 않는다.
- 표기: **누적 거리**(km, 소수 1자리) · **누적 시간**(시간·분) · **누적 상승고도**(m, 있을 때만) · **누적 칼로리**(kcal, 있을 때만). 문구 예: "이 장비와 87.3km를 걸었어요".
- 거리 문구가 카드 첫 줄(`LiquidType.heading`)이고 나머지 지표는 그 아래 `라벨 · 값` 행으로 헤어라인에 갈린다 — 스펙 표(GD-8)와 같은 행 문법이다.
- 심박·경로는 서버에 없으므로(HA-5) 다루지 않는다.

### GD-12 덜어내기 시그널 `[제안]`

앱 정체성(useless)의 핵심 — 데이터가 "덜어낼 후보"라고 말할 때 조용히 알려준다.

**수용 기준**

- **표시 조건**: 기록된 여행(`used+useless`)이 **3회 이상**이고, **기록 기준 최근 3회가 모두 '사용 안함'**일 때. (최근 판정은 해당 배낭들의 `startDate` 내림차순 기준, 날짜 없으면 `editDate`.)
- 배너 문구: "최근 3번의 여행에서 쓰지 않았어요 — 배낭에서 빼면 **−{weight}g**" (무게 없으면 무게 문구 생략). 무게 표기는 목록 행과 같다(DM-26 — 2026-08-11 개정: 그전에는 여기만 천 단위 구분자를 넣어 같은 값이 `7,200g`으로 보였다).
- 정보성 배너다 — 탭 액션 없음, 삭제 유도 아님(파괴적 행동 버튼 금지). 시각은 경고 빨강(`LiquidSemantic.danger`)이 아니라 차분한 경고 면 — `LiquidSemantic.warnBg` 면 + `warnInk` 아이콘·글자, radius `LiquidRadius.tile`. 의미색은 액센트 체계 밖이라 리디자인해도 값이 바뀌지 않는다.
- 조건 미충족이거나 기록 부족 시 렌더하지 않는다.

### GD-3 리뷰 미리보기 섹션

**수용 기준**

- 댓글이 없으면 `첫 번째 리뷰 남기기` 버튼 → 누르면 댓글 목록 화면으로 이동([Reply.md](Reply.md)).
- 댓글이 있으면 최신 댓글 1개(`createdAt desc limit 1`)와 `더 많은 의견 보기` 버튼을 표시한다.
- 리뷰 화면 이동은 로그인 확인을 거친다.
- 평점 요약(별 + 평균 + `(리뷰 N)`)은 **섹션 라벨 우측**에 붙는다 — 제목과 짝지어 읽히는 값이라 카드 안으로 넣지 않는다. 평균값은 콘덴스드(`LiquidType.numSm`)를 쓴다(숫자 전용).
- 댓글 행·`더 많은 의견 보기`·`첫 번째 리뷰 남기기`는 **흰 카드 하나** 안의 행이며 헤어라인으로 갈린다(2026-08-11 — 그전에는 행마다 종이 면). 이동 행은 좌 텍스트 · 우 쉐브론이고 세로 패딩으로 44pt 터치 타깃을 만든다.
- **0건일 때도 섹션과 카드를 유지한다**(2026-08-11 디자인 리뷰에서 축약 여부를 검토한 뒤 유지 결정). 리뷰가 0건인 상태에서 이 카드 한 칸이 곧 **유일한 기여 진입점**이고, 다른 빈 섹션(GD-9 `사용 기록`)도 조용한 카드 한 장으로 같은 조판을 쓴다 — 여기만 줄이면 빈 섹션의 규칙이 화면 안에서 갈린다. 행이 하나(`첫 번째 리뷰 남기기`)뿐이라 카드 높이는 이미 최소치다.

### GD-4 공유 이미지 갤러리 `[폐기]`

**장비 이미지 미제공 원칙([DataModel.md](DataModel.md) §1, 2026-07-28)에 따라 제거한다.**
갤러리 모달(`SharedImageSelectionModalView`)·업로드·삭제·대표 사진 변경(`users/{uid}/gears/{id}.imageUrl` 갱신)과
진입 버튼, 관련 코드(`components/gear-image/`, `model/gear-image/`, `model/store/GearImageStore.ts`)를 모두 걷어낸다.
기존 데이터(DM-8)·Storage 파일 정리는 DataModel §1 운영 절차. `GD-4` 번호는 재사용하지 않는다.

### GD-5 외부 링크 (브랜드 공식몰 · 쿠팡 파트너스)

사용자는 카탈로그 장비 상세에서 **브랜드 공식몰 상품 페이지**와 **쿠팡 파트너스 최저가**로 바로 이동할 수 있다.

**2026-08-08 — 브랜드 공식몰 링크(`productUrl`)를 더한다.** 지금까지 이 섹션은 쿠팡 링크 하나였는데, 실데이터에서 `coupangUrl`은 **27건(0.1%)** 뿐이고 `productUrl`은 **37,576건(90.8%)** 이다([DataModel.md](DataModel.md) DM-3). 사용자가 보는 장비 대부분에 아무 외부 링크가 없었던 셈이다.

**수용 기준**

- 섹션 소제목은 **`공식 링크`** 이며 다른 섹션(스펙·리뷰)과 같은 위계·여백을 쓴다 — **대문자 마이크로 라벨**(`LiquidSectionLabel`) + 섹션 사이 `LiquidLayout.section`(26). 소제목이 없으면 앞 섹션에 딸린 것처럼 읽힌다. (2026-08-11 개정: 그전에는 17pt semibold + `marginVertical: 20`이었다. 이 화면에서 이제 **모든 섹션이 같은 마이크로 라벨**을 쓴다 — 제목 없는 섹션은 없다.)
- 두 링크 모두 **행 하나씩**으로 낸다. 둘 다 있으면 두 행, 하나만 있으면 그 행만, 둘 다 없으면 섹션 자체를 렌더하지 않는다.
- 차례는 **브랜드 → 쿠팡**이다. 커버리지가 90.8% 대 0.1%라 브랜드 행이 사실상 기본이고 쿠팡은 예외다 — 기본을 위에 두지 않으면 대다수 장비에서 섹션 첫 줄이 비는 셈이 된다.
- **브랜드 행 라벨은 링크 대상이 공식몰인지에 따라 갈린다**(`model/gear/GearBrandLink.ts`):
  - **공식몰**: `{브랜드} 공식몰에서 보기`. 브랜드명이 비면 `공식몰에서 보기`.
  - **그 외(편집숍 등)**: `{도메인}에서 보기` (예: `thegearshop.co.kr에서 보기`). 도메인도 못 얻으면 `상품 페이지 보기`.
  - 판정은 **호스트에 브랜드 슬러그(`company`)가 들어 있는지**로 한다. 실데이터 23개 도메인 중 21개가 이 방식으로 100% 맞고, 최다 도메인인 더 기어샵(11,806건·31%)은 아크테릭스·스카르파 등을 함께 파는 **편집숍**이라 0%로 갈린다 — 여기에 `아크테릭스 공식몰`이라 쓰면 사실이 아니다.
  - 슬러그가 약어라 공식몰을 못 맞히는 경우(`hmg` vs `hyperlitemountaingear.co.kr`, 102건)는 중립 라벨로 떨어진다. **틀리는 방향을 이쪽으로 고정한 것** — 편집숍을 공식몰이라 부르는 쪽이 더 나쁘다.
- **브랜드 행은 상품 이미지가 있는 링크 미리보기 카드로 낸다**(2026-08-09, 아래 §GD-5a).
- **파트너스 고지는 쿠팡 행에만 딸린다.** 브랜드 링크는 수수료와 무관하므로 고지가 그 아래로 내려가면 사실과 다르다.
- 두 링크는 **같은 카탈로그 문서 한 번 읽기**로 함께 가져온다(`GearStore`의 기존 캐시 재사용) — 링크 하나 늘었다고 Firestore 읽기가 늘어나면 안 된다.
- 카탈로그 `gear/{id}.coupangUrl`이 있을 때만 `쿠팡에서 최저가 보기` 링크를 노출한다. 값이 없으면 그 행과 고지 문구 모두 미노출.
- 배치는 **보유 여부로 분기**한다 — 이 화면은 보유자(관리)와 탐색자(쇼핑) 겸용이므로 각 모드의 관심사를 위로 올린다:
  - **미보유(카탈로그 진입)**: 현행대로 제품 정보 바로 뒤 (구매 판단 동선).
  - **보유(`isAdded`)**: 배낭 기록 섹션 **뒤**, 리뷰 섹션 위 (내 기록이 최저가 링크보다 우선).
  - 어느 위치든 **독립 섹션**이며, 다른 섹션과 같은 방식으로 갈린다 — 대문자 마이크로 라벨(`LiquidSectionLabel`) + 섹션 사이 `LiquidLayout.section`(26). (2026-08-11 개정: 그전에는 섹션 상단에 10px 회색 밴드(`components/ui/SeperaterView`)를 넣었다. Liquid Depth에서 구획은 띠가 아니라 여백과 면이 맡으므로 그 컴포넌트는 삭제됐다.)
- 링크는 **항상 카탈로그 `gear/{id}`에서만** 읽는다. 커스텀 장비는 카탈로그 문서가 없어 자연스럽게 미노출되고, 창고 사본으로 진입해도 카탈로그에서 읽는다.
- 링크를 누르면 `click_gear_purchase` 이벤트(`source`: `coupang` \| `brand`)를 전송한 뒤 외부 링크를 연다. 열기 실패는 조용히 무시한다.
- **여는 방식은 행마다 다르다** — 목적이 다르기 때문이다:
  - **브랜드 행: 인앱 브라우저**(`expo-web-browser`의 `openBrowserAsync`). 제품 정보를 확인하고 앱으로 돌아오는 동선이라 앱을 떠나게 할 이유가 없다. 시트를 닫으면 장비 상세로 복귀한다.
  - **쿠팡 행: `Linking.openURL` 유지.** 파트너스 링크(`link.coupang.com/a/...`)는 유니버설 링크라 쿠팡 앱이 깔려 있으면 앱으로 넘어간다 — 인앱 브라우저로 감싸면 그 전환이 막혀 파트너스 수수료 귀속에 불리하다.
- 쿠팡 행 바로 아래에 수수료 고지 문구(`쿠팡 파트너스 활동의 일환으로, 이에 따른 일정액의 수수료를 제공받습니다.`)를 상시(숨김/접힘 없이) 표시한다.
- 고지 문구는 상시 노출을 유지하되 링크보다 시각 위계를 낮춘다(fontSize 11, `textSecondary`) — 법적 고지가 링크 본문보다 무거워 보이지 않게.

### GD-5a 브랜드 링크 미리보기 카드

**2026-08-09 결정.** 브랜드 행을 가운데 정렬 텍스트 링크에서 **링크 미리보기 카드**로 바꾼다. 카카오톡·슬랙이 링크를 붙였을 때 보여주는 그 카드와 같은 성격이다.

**수집은 미리 하지 않는다 — 처음 열릴 때 한 번 가져오고 그 값을 저장한다.**

```
1회차: 상세 진입 → 상품 페이지 og:image 읽기 → 표시 → gear/{id}.productImageUrl에 기록
2회차~: gear 문서를 읽을 때 URL이 함께 실려 옴 → 즉시 표시, 추가 요청 0
```

전량 백필(37,382건·약 1시간)을 하지 않는 이유는 **필요가 없어서**다. 실제로 열리는 장비만 채워지면 되고, 장비 상세는 어차피 `gear/{id}` 문서를 한 번 읽으므로(쿠팡·브랜드 링크) **조회 시점 추가 비용이 0**이다. 반대로 매 조회마다 og를 긁는 방식은 브랜드 서버를 조회수만큼 때린다 — 저장 방식은 **상품당 사실상 1회**다.

`<head>`만 잘라 받는 안(HTTP Range)도 검증했으나 **상위 7개 도메인 중 1개(cayl.co.kr)만 206을 준다**. 나머지는 동적 페이지라 범위를 못 자르고 전체(179~332KB)를 보낸다.

**수용 기준**

- **이미지가 있으면 상단 이미지 밴드(180pt) + 하단 텍스트 줄**, 없으면 **좌측 아이콘 타일 + 텍스트 한 줄**로 떨어진다. 빈 밴드를 남기지 않으려고 형태를 나눈다.
- 밴드는 `resizeMode: contain` · 흰 배경이다. 상품 사진이 대부분 흰 배경 정사각이라 좌우 letterbox가 사진 배경과 이어져 보이지 않는다.
- **로딩 구간이 둘이고 둘 다 스켈레톤으로 덮는다** — ① og 수집(URL을 아직 모름), ② 이미지 다운로드(URL은 알지만 아직 안 그려짐). 수집 단계부터 밴드 자리를 잡아둬야 이미지가 도착할 때 **카드 높이가 튀지 않는다**. 캡션 줄은 처음부터 실제 텍스트로 그린다.
- 로드에 실패하면 스켈레톤을 걷고 **아이콘 형태로 떨어진다** — 깨진 썸네일을 남기지 않는다.
- 이미지는 **브랜드 CDN URL로만** 렌더한다. 우리 Storage 사본(`imageUrl`)으로 대체 금지([DataModel.md](DataModel.md) §1 경계).
- **수집은 네이티브에서만** 한다 — 웹은 남의 사이트 HTML 읽기를 CORS가 막는다. 웹은 네이티브가 채워 둔 값을 읽기만 하므로 시간이 지날수록 커버리지가 따라 올라간다.
- **한 세션에서 같은 장비를 다시 시도하지 않는다.** 죽은 링크가 27%가량이라 재시도는 곧 낭비다.
- **사이트 공용 로고는 거른다.** 시에라디자인은 상품 637건이 전부 같은 `og.jpg`(사이트 로고)를 반환한다 — 그리면 미리보기가 없느니만 못하다. 알려진 URL을 코드 상수(`GENERIC_IMAGES`)로 제외한다.
- **도메인 차단 스위치**를 코드 상수(`BLOCKED_HOSTS`)로 둔다. 브랜드 이의·Referer 차단 시 여기에 추가하고 **OTA로 내보낸다** — 스토어 심사 없이 반영된다. (Firestore 원격 설정 안은 보안 규칙을 함께 열어야 하고, 규칙이 없으면 조용히 무력해져서 접었다.)
- **출처 도메인을 노출한다**(`snowpeak.co.kr`). 미리보기의 핵심 정보이고 §1 경계 조건 ③이다.
  - 단 라벨이 이미 도메인인 경우(공식몰이 아닐 때)는 **도메인 줄을 생략한다** — 같은 문자열이 두 번 나오면 지저분하다.
- 카드 전체가 하나의 터치 타깃이며 동작은 기존 브랜드 행과 같다(`click_gear_purchase` `source: brand` → 인앱 브라우저).
- 쿠팡 행은 카드로 바꾸지 않는다 — 파트너스 고지가 딸려 구조가 다르고 27건(0.1%)뿐이라 통일할 실익이 없다.

**검증 체크리스트**

- [ ] 수집 전 장비를 처음 열면 → 밴드가 스켈레톤으로 뜬 뒤 이미지가 채워지고(카드 높이 변화 없음), `productImageUrl`이 Firestore에 기록된다 (GD-5a)
- [ ] 같은 장비 재진입 → 추가 og 요청 없이 즉시 이미지 표시 (GD-5a)
- [ ] 이미지 URL이 404/차단 → 깨진 썸네일 없이 상점 아이콘 (GD-5a)
- [ ] 공식몰 링크 → `{브랜드} 공식몰에서 보기` + 도메인 2줄 (GD-5a)
- [ ] 편집숍 링크 → `{도메인}에서 보기` 1줄, 도메인 중복 없음 (GD-5a)
- [ ] 카드 탭 → 인앱 브라우저, 닫으면 장비 상세 복귀 (GD-5a)
- [ ] 섹션 소제목 `공식 링크`가 스펙·리뷰와 같은 위계로 보인다 (GD-5)
- [ ] 웹에서 → 수집은 안 하지만 이미 채워진 장비는 이미지가 보인다 (GD-5a)
- [ ] 카드가 우리 Storage `imageUrl`을 **절대** 렌더하지 않는다 (GD-5a)

### GD-6 외부 후기 (네이버 블로그·유튜브)

사용자는 장비 상세에서 해당 장비의 외부 후기(블로그 글·유튜브 영상)를 바로 확인할 수 있다. 박지 상세(CS-3)의 후기 섹션과 동일한 공용 모듈을 재사용한다.

**수용 기준**

- 공용 후기 모듈(`components/review/ReviewSectionView.tsx`)은 2026-08-11에 Liquid Depth로 이식됐다 — 시각 문법은 [CampSite.md](CampSite.md) CS-3의 `모듈 문법` 항목이 정본이며, 이 화면은 섹션 껍데기(`variant='list'`)만 제공한다.
- 섹션 타이틀은 `블로그·유튜브 후기`(앱 내 사용자 `리뷰` 섹션과 구분). **배치는 보유 여부로 분기**한다 — 미보유(쇼핑 맥락)는 외부 후기가 구매 판단에 더 유용하므로 리뷰(댓글) 섹션 **위**, 보유는 리뷰 섹션 아래.
- **검색어는 소스별로 다르게 조립한다** — 두 소스의 검색 특성이 달라 같은 문자열을 쓰면 한쪽이 망가진다(캐시 100건 실측: 블로그는 제품명 일치 93.3%인데 유튜브는 56.3%).
  - **두 소스 공통**: 검색어에 넣는 장비명은 **일반 토큰(사이즈·성별)을 제거한 형태**를 쓴다. 사이즈까지 넣으면 검색이 과하게 좁아져 후보 자체가 사라진다 — 라이브 실측: `"몽벨 montbell 버사자켓 남 미디엄"`은 유튜브 후보 **0건**, `"몽벨 montbell 버사자켓"`은 **25건**(블로그도 6건 → 30건). 단 **모델 번호처럼 1자인 토큰은 검색어에서 빼지 않는다**(`에어로라이트 2`와 `3`이 구분돼야 한다) — 길이 규칙은 필수 토큰 판정에만 적용된다.
  - **블로그**: `"{제조사 표시명} {일반 토큰 제거한 장비명} 후기"`(표시명 = `getDisplayName()` 계열, 제조사 없으면 생략).
  - **유튜브**: `"{제조사 표시명} {제조사 영문} {일반 토큰 제거한 장비명}"`. ① **`후기`를 넣지 않는다** — `추천 판매순위 Top10 ... 후기 비교` 류 리스티클이 여러 장비에 공통으로 붙는 원인이다. ② **영문 제조사(`getCompany()`)가 표시명과 다르면 함께 넣는다** — 카탈로그 `companyKorean`이 실제로 아무도 쓰지 않는 음차인 경우가 많다(`에이엠지티타늄` ↔ 실제 표기 `AMG티타늄`/`AMG-TITANIUM`).
- **유튜브 영상**(상위 4건, 가로 카드)과 **네이버 블로그**(상위 5건, 리스트) 구성·동작은 CS-3와 동일 — 후보 확대(`maxResults=25`/`display=30`) 후 **관련성 필터**를 적용해 상위 건수만 남기고, 유튜브에 `relevanceLanguage=ko`·`regionCode=KR`을 함께 보낸다(규칙은 CS-3 `관련성 필터` 항목이 단일 소스). 탭 시 외부 앱/브라우저로 이동, HTML 태그·엔티티 제거, 소스별 실패·0건이면 해당 리스트만 생략(둘 다 0건이면 섹션 생략).
  - 장비(GD-6)의 **필수 토큰은 장비명 토큰만** 쓴다 — `getDisplayName()`과 `getName()`을 각각 공백으로 분리한 2자 이상 토큰에서 사이즈·성별 등 **일반 토큰**(`mens`·`남`·`엑스라지`·`s`/`m`/`l` 등, 목록은 공용 모듈에 둔다)을 뺀 집합. **제조사만 일치하는 항목은 통과시키지 않는다** — 브랜드는 맞고 제품이 다른 결과(`몽벨 버사자켓` 상세에 `몽벨 아울렛 득템후기`)가 사용자에게 가장 무관하게 읽힌다. 토큰이 비었을 때의 처리는 아래 **필수 토큰 폴백**이 규정한다.
  - **판정 전용 제외 목록에는 카테고리·품목 명사(`텐트`·`배낭`·`필로우`·`매트`·`파우치` 등)도 넣는다.** 카테고리 명사만으로 판정하면 **같은 품목의 다른 제품**이 그대로 통과한다 — 라이브 실측: `엑스패드 렘 필로우` 상세에서 필수 토큰이 `["필로우"]`뿐이라 `엑스패드 딥슬립 필로우`·`랩 스트라토스피어 필로우`(타사 제품)가 통과했다. 모델명이 함께 있는 장비(`스텔라릿지 텐트`)는 모델명 토큰이 판정을 계속 담당하므로 동작이 바뀌지 않는다. **단 카테고리 명사는 검색어에서는 빼지 않는다**(위 검색어 규칙은 사이즈·성별만 제거) — 빼면 `엑스패드 렘`처럼 품목이 사라진 문구가 되어 후보를 못 찾는다. 즉 제외 목록은 **사이즈·성별(검색어·판정 공통)** 과 **카테고리 명사(판정 전용)** 두 벌이다.
  - **필수 토큰 폴백**: ① 이름(표시명·캐논컬명)**별로** 낱말 토큰을 뽑고, **어떤 이름이 낱말 토큰을 하나도 못 내면 그 이름의 전체 토큰**(사이즈·성별 낱말을 뺀 나머지를 공백 없이 이어 붙인 형태 — 제목에 사이즈를 적지 않으므로 `콕 파우치 - S`를 `콕파우치s`로 만들면 아무 제목에도 걸리지 않는다)을 대신 넣는다 → ② 그렇게 모은 집합이 그래도 비면 제조사 토큰(제조사는 낱말로 쪼개지 않고 문자열 전체를 한 토큰으로 — 쪼개면 `노스` 같은 조각이 무관한 제목에 걸린다).
    - 전체 토큰이 필요한 이유는 `렘`처럼 **1자 고유 모델명이 길이 규칙(2자)에 걸려 버려지기** 때문이다. 라이브 실측에서 `["필로우"]` → 블로그 통과 20건(대부분 타사 제품)이 `["렘필로우"]` → **3건 전부 해당 제품**으로 바뀌었다.
    - **이름별로 판단하는 이유**: 전체 집합이 빌 때만 폴백하면 한글 표시명이 토큰을 못 내도 **영문 캐논컬명의 로마자 토큰이 남아 폴백을 건너뛴다.** 라이브 카탈로그 11건이 이 상태다(`돔 3 텐트 매트`/`Dome 3 Tent Mat` → `["dome"]`, `콕 파우치 - S`/`Kok Pauchi - S` → `["kok","pauchi"]`). 한국어 제목은 이 토큰에 걸리지 않아 **후보가 전량 탈락하고 섹션이 사라진다.**
    - 제조사 폴백을 마지막에 두는 이유는 그것이 `브랜드만 일치` 문제를 되살리기 때문이다.
- **후기 캐시(주 1회 최신화)**: Firestore `gear-review/{gearId}`([DataModel.md](DataModel.md) DM-19)에 저장. 정책은 DM-18과 동일 — 캐시 우선 표시, `updatedAt` 7일 초과 시 재조회, 두 소스 모두 성공 시에만 갱신, 실패 시 기존 캐시 유지. `queryVersion` 불일치 캐시는 TTL과 무관하게 만료·미표시(DM-18/19).
- 블로그·유튜브 클릭은 `click_gear_review`(파라미터 `source: blog|youtube`)로 계측한다.

### GD-7 공유 `[제안]`

사용자는 카탈로그 장비를 외부로 공유하고, 받은 사람은 웹 랜딩을 거쳐 앱으로 진입한다(박지 공유 CS-7과 동일 구조).

**수용 기준**

- **카탈로그 장비(`!isCustom`)** 상세 헤더 우측에 공유 아이콘(수정하기 왼쪽). 커스텀 장비는 웹 랜딩 대상이 아니라 노출하지 않는다.
- 탭 → OS 공유 시트(`Share.share`)로 URL `{웹 베이스}/gear-share/{encodeURIComponent(gearId)}`**만** 내보낸다. 웹 베이스는 `constants/WebLinks.ts`의 `WEB_BASE_URL` 단일 소스를 쓴다(2026-07-28 커스텀 도메인 `useless.my` → Firebase Hosting 기본 도메인 `https://lessismore-7e070.web.app`). 공유 클릭은 `click_gear_share`로 계측한다.
- **웹 랜딩**(별도 레포 `lessismore`, `/gear-share/:id`): Firestore `/gear/{id}`(공개 읽기)를 읽어 제조사·이름·무게·카테고리를 보여주고(장비 이미지 미제공 원칙 — 사진 없음), `앱에서 보기` 버튼으로 `lessismoreapp://gear-detail/{id}` 딥링크(미설치 시 스토어 폴백)로 앱을 연다.

### GD-8 카테고리별 스펙 표 `[기획]`

크롤 파이프라인이 기록한 `gear.specs`(DM-3)를 카테고리별 스키마에 맞춰 상세에 표시한다.

**수용 기준**

- 기본 정보 아래(배낭 기록/최저가 위)에 **`스펙` 섹션**을 두고, `specs`에 값이 있는 필드만 `라벨 : 값` 2열 표로 표시한다. `specs`가 없거나 전부 비면 섹션 자체를 렌더하지 않는다(레거시·커스텀 장비 호환).
  - 목업 §9는 `사용 기록`을 `스펙`보다 위에 그렸지만 **차례는 이 항목이 정본**이다 — 보유 장비에서 스펙이 배낭 기록 아래로 내려가면 미보유(쇼핑) 진입과 섹션 순서가 갈린다.
- 스키마 사본 `model/gear/GearSpecsSchema.ts`가 계약: 웹 `specs-schema.js`와 **필드 키·한글 라벨·단위·타입을 동일하게** 유지한다(카테고리 33키). 스키마에 없는 미지의 키는 키명 그대로 표시(값 문자열화).
- 값 포맷: boolean은 `예`/`아니오`, `unit` 있으면 `값+단위`(예: `1500mm`, `850FP`, `-5°C`), enum은 한글 표시(`down`→`덕다운/합성` 등 라벨은 스키마에 정의), 빈 문자열·null은 해당 행 생략.
- 표는 흰 카드 하나(radius `LiquidRadius.card`, 좌우 여백 16, `shadow card`)에 담는다. **라벨은 96pt 고정 폭** `inkMuted` 13.5, 값은 남는 폭 `ink` 13.5/500이고 행 사이는 헤어라인이다 — 라벨 폭을 고정해야 값의 왼쪽 선이 모든 행에서 같아 표로 읽힌다. 구분선은 카드 안쪽 여백을 그대로 가로지른다(목록 행처럼 들여쓰지 않는다 — 라벨 컬럼이 이미 왼쪽 선을 만든다).

### GD-14 하단 고정 CTA (as-built 2026-08-11)

이 화면의 **주 액션은 하나**이고 하단에 고정된다. 라벨·동작은 기존과 같고(진입 맥락으로 갈린다) 시각만 정의한다.

**수용 기준**

- 라벨은 배낭 컨텍스트면 `이 배낭에 담기`, 아니면 `내 창고에 추가하기`다(GE-8). **이미 보유했거나 이미 그 배낭에 담긴 장비면 버튼 자체를 그리지 않는다** — 이때 화면에는 고정 액션이 없다.
- 표현은 `LiquidPillButton`(잉크 면, h54, 알약, `shadow cta`) **폭 채움**이고, 위·아래 여백은 12/10 + 세이프에어리어다.
- 버튼 뒤는 **지면색 그라디언트**(`LiquidBackdrop.ctaVeil` — 아래는 `canvas` 꽉, 위로 갈수록 투명)로 받친다. 불투명 띠를 두면 버튼 주위에 각진 면이 생겨 카드가 그 뒤에서 잘려 보인다(2026-08-04 배낭 상세에서 확인한 것과 같은 이유).
- 담기 요청 중에는 버튼을 **비활성**(투명도만 낮춘다) + 라벨 앞 라임 스피너로 두고 재탭을 막는다.
- 스크롤 콘텐츠는 이 블록 높이 + 여유만큼 하단을 비워, 마지막 카드가 그라디언트에 물리지 않는다.

## 4. 데이터

- 읽기: 장비 문서(DM-3), 최신 댓글(DM-7), 외부 후기 캐시(DM-19).
- 쓰기: 외부 후기 캐시 갱신(DM-19).

## 5. 플랫폼 분기

없음 — 이미지 업로드/삭제 선택 UI 분기는 GD-4 폐기(2026-07-28)와 함께 제거.

## 6. 엣지 케이스

- **장비 조회 실패**(삭제된 장비 등): 알럿 표시 후 초기화 중단. 화면은 스켈레톤에 머무르므로 **유리 back으로 빠져나갈 수 있어야 한다**(아래 로딩 항목).
- **로딩**: 초기화 완료 전에는 **스켈레톤**을 그린다(2026-08-11 개정 — 그전에는 `null`이라 빈 지면이었다). 도착할 화면과 같은 골격(정체 블록 → 섹션 라벨 → 카드 행)이고 셔머는 왕복 1.2s다 — 이 앱은 로딩에 스피너를 쓰지 않는다(디자인 핸드오프 Interactions).
  - **이 구간에도 유리 back은 전 플랫폼에서 살아 있다** — iOS는 네이티브 투명 헤더가, Android·Web은 본 화면과 같은 `LiquidHeaderChrome`이 낸다. 액션(공유·수정)은 아직 장비를 몰라 넘기지 않으므로 **유리 캡슐 자체가 그려지지 않고**(빈 알약을 남기지 않는다), 타이틀도 비운다.
  - 스켈레톤의 정체 블록 상단 여백은 본 화면과 **같은 구조로 더한다** — iOS 헤더 인셋(`insets.top + navBar`) **위에** 콘텐츠 첫 블록의 14를 얹는다. 인셋으로 덮어 쓰면 로드 직후 정체 블록이 14pt 튄다.
- **비로그인**: 리뷰 이동 시 로그인 유도.

## 7. 수동 검증 체크리스트

- [ ] 창고/검색 양쪽에서 진입 시 동일 장비 정보 표시
- [ ] 배낭에 담긴 적 없는 장비 → 배낭 기록 섹션 미노출
- [ ] 진입 직후 → 스켈레톤(셔머)이 뜨고, 로드되면 정체 블록 자리가 튀지 않는다 (§6)
- [ ] 최상단에서 헤더에 타이틀 없음 → 정체 블록을 지나면 제품명 표시 → 최상단 복귀 시 사라짐 (GD-1)
- [ ] 화면에 라임 면이 **하나**(사용 지표 첫 타일)뿐이고, 잉크 CTA도 하나다 (§0)
- [ ] `사용`이 0인 장비 → 그 타일에 라임 면이 없고(흰 면 + 낮춘 숫자) 화면에 라임 면이 아예 없다 (GD-9)
- [ ] 이름 바로 아래 좌측에 무게 한 줄(`1510g`)이 있고, 숫자와 `g`가 같은 크기·같은 베이스라인이다 (GD-1)
- [ ] 전체 주소가 저장된 배낭 → 여행 카드 여행지가 시·군·구까지만 나오고 단어 중간에서 잘리지 않는다 (GD-10)
- [ ] 미보유 장비 → 하단 CTA가 지면색 그라디언트 위에 떠 있고, 스크롤 끝에서 마지막 카드가 버튼에 가리지 않는다 (GD-14)
- [ ] 담기 탭 → 버튼이 비활성 + 라임 스피너, 연타해도 한 번만 담긴다 (GD-14)
- [ ] 보유 장비인데 사진이 없으면 → 상세에 사진 칸·점선 박스가 **없고**, 정체 블록 다음이 곧 스펙/사용 기록이다 (GD-1·GD-13)
- [ ] `수정` 진입 → 폼 마지막에 `사진` 필드(없으면 점선 `사진 추가`, 있으면 140pt 정사각 탭 → 교체/삭제) (GD-13)
- [ ] Android/Web → 유리 원형 back + 유리 캡슐 `[공유][수정]`이 iOS 바 버튼과 같은 순서다 (§0)
- [ ] 로딩 구간에도 유리 back으로 빠져나갈 수 있다 (iOS 네이티브 헤더 / Android·Web 유리 크롬, 캡슐은 없음) (§6)
- [ ] 커스텀 + 미보유 장비 → 헤더에 빈 유리 캡슐이 남지 않는다 (GD-1)
- [ ] 배낭에 담은 적 없는 보유 장비 → `사용 기록`에 두 줄 안내(사실 + 다음 걸음)가 뜬다 (GD-9)

### GD-13 내 장비 사진 업로드 `[제안]`

사용자는 자기 장비의 사진을 직접 올려 창고·배낭에서 알아보기 쉽게 만들 수 있다. **본인만 보는 사진**이다([DataModel.md](DataModel.md) §1, 2026-07-29 개정).

**수용 기준**

- **보유 장비(`isAdded`)에만** 업로드 UI를 둔다. 카탈로그로만 진입한 미보유 장비에는 노출하지 않는다 — 저장할 문서(`users/{uid}/gears/{id}`)가 없다. `수정`이 보유 장비에만 뜨므로(GD-1) 이 조건은 진입점 자체로 지켜진다.
- **사진을 관리하는 자리는 `수정` 폼 하나다**(2026-08-11 디자인 리뷰 — `components/gear/edit/GearEditPhotoView.tsx`, [GearEdit.md](GearEdit.md) GE-2). 장비 상세는 사진을 **보여주기만** 하고(GD-1 1층) 업로드 UI를 두지 않는다.
  - **왜 옮겼나**: 상세 두 번째 블록에 전체 폭 점선 박스가 놓여 **화면에서 가장 큰 요소가 콘텐츠 0인 빈 칸**이었다. 이 앱은 장비 이미지를 취급하지 않기로 한 제품이라([DataModel.md](DataModel.md) §1) 사진 없는 장비가 기본 모습이고, 그 자리는 대다수 사용자에게 영구히 비어 있다. 같은 자리를 무게(GD-1)와 사용 기록(GD-9)이 가져간다. 업로드 기능 자체는 유지하고 **진입점만** 옮겼다.
  - 진입점: 폼의 **마지막 필드**(`사진`)에 둔다 — 이름·브랜드·무게가 장비를 정하는 값이고 사진은 있으면 좋은 값이라 판단 순서대로 아래다. 사진이 없으면 **점선 슬롯**(h56 하한 · radius `LiquidRadius.tile` · 1.5px dashed `Liquid.dashedStroke` · `camera-outline` + `사진 추가` `inkMuted`), 있으면 140pt 정사각을 탭해 **교체/삭제 액션 시트**를 연다.
  - 슬롯 높이는 고정이 아니라 **하한**이다 — OS 글자 크기를 키우면 행이 함께 늘어난다(Dynamic Type).
  - **사진은 폼 저장(`저장하기`)과 무관하게 고른 즉시 저장된다** — Storage 업로드와 문서 갱신이 한 흐름이라(DM-9) 확정 시점을 폼 저장까지 미루면 올린 파일이 참조 없이 남는다. `GearStore.update`는 부분 갱신이라(DM-11) 폼을 저장해도 방금 올린 `imageUrl`을 덮지 않는다.
  - **[이력] 2026-07-29~2026-08-11**: 진입점이 장비 상세 정체 블록 **아래** 전체 폭 점선 슬롯이었다(그전엔 정체 블록 위). 사진이 생기면 그 자리가 사라지고 정체 블록 위 가운데 사진 줄이 생기는 구조였다.
- 사진 출처는 **앨범 선택 + 카메라 촬영** 두 갈래를 제공한다(`expo-image-picker` — 필름 카드 `model/bag-film-card/BagFilmCard.ts`가 이미 쓰는 의존성 재사용).
  - **카메라를 쓰므로 `app.json`의 `expo-image-picker` 플러그인에 `cameraPermission` 한국어 문구가 있어야 한다.** 2026-07-28 제거 때 빠져 있었고, 없으면 iOS `NSCameraUsageDescription`에 영문 기본값이 박혀 심사 리젝 사유가 된다. `photosPermission`도 장비 사진 맥락을 포함하도록 갱신한다.
  - **권한 문구는 네이티브 Info.plist 값이라 OTA로 나가지 않는다 — 이 기능은 새 EAS 빌드가 필요하다.**
- **권한 거부** 시 알럿으로 안내하고 설정 이동 경로를 제시한다. 권한 거부는 실패로 처리하되 에러 문구를 띄우지 않는다(사용자의 선택).
- 업로드 흐름: 선택 → **업로드 중 표시**(진행 인디케이터, 화면 이탈 금지 아님) → Storage `/{userId}/{fileName}` 업로드(DM-9) → 다운로드 URL을 `users/{uid}/gears/{id}.imageUrl`에 저장 → 화면 즉시 반영.
  - 업로드 중 표시는 슬롯 안 아이콘 자리를 `ActivityIndicator`로 바꾸고 문구를 `사진 올리는 중`으로 둔다. **"로딩에 스피너를 쓰지 않는다"(핸드오프)는 이 자리에 적용되지 않는다** — 그 규칙은 _도착할 화면을 기다리는_ 구간을 스켈레톤으로 그리라는 것이고(§6 로딩), 여기는 사용자가 방금 누른 요청이 **아직 오가고 있다**는 인플라이트 표시다. 자리가 이미 있으므로 스켈레톤으로 그릴 골격도 없다. 담기 CTA의 라임 스피너(GD-14)도 같은 예외다.
- **교체·삭제 시 이전 Storage 파일을 함께 지운다**(DM-9). 삭제는 확인 다이얼로그를 거치고, 확정 시 `imageUrl`을 제거한다.
- **업로드 실패**(네트워크·용량)는 토스트로 알리고 기존 상태를 유지한다. 부분 실패로 `imageUrl`이 깨진 URL을 가리키지 않게, **Storage 업로드 성공 후에만** 문서를 갱신한다.
- 이미지 로드 실패(파일이 지워진 레거시 URL 등)는 **이미지 없음과 동일하게** 처리한다 — 깨진 아이콘을 남기지 않는다. 2024~2026 업로드분 중 참조가 끊긴 것이 있을 수 있다.
- **장비를 수정해도 사진이 지워지지 않아야 한다.** 원인이 둘이라 양쪽을 막는다 — ① `GearStore.update`가 문서를 통째로 덮어쓰던 것을 **부분 갱신으로 바꾼다**([DataModel.md](DataModel.md) DM-11), ② 편집 폼이 `getExtra()`로 `Gear`를 재구성하므로 `Gear.getData()`에도 `imageUrl`을 포함시킨다. 폐기 기간에 실제로 ①번 경로로 값이 유실될 수 있었다(§1 — 배포 전이라 실제 유실은 없었다).
- 업로드한 이미지는 **창고 목록**([Warehouse.md](Warehouse.md) WH-1)과 **배낭 상세 장비 행**([BagDetail.md](BagDetail.md) BD-1)에도 나타난다. **공유 배낭·후기 첨부 배낭 등 제3자 표면에는 나타나지 않는다**(§1 비공개 원칙).

### GD-9~12 사용 인사이트

- [ ] 담김/사용/사용 안함/미기록 수치가 실제 기록과 일치(미기록 0이면 숨김)
- [ ] 여행 타임라인: 최신순 정렬, 여행지·기간·날씨 표시(없는 값 생략), 3-상태 태그(챙김 표시 없음)
- [ ] 사용한 여행에 운동 기록 있으면 누적 거리·시간(·상승·칼로리) 합산 표시, 없으면 섹션 숨김
- [ ] 최근 기록 3회 연속 사용 안함 → 덜어내기 배너(−Ng), 조건 미충족 시 미노출
- [ ] 미보유(카탈로그) 장비 → GD-9~12 전부 미노출
- [ ] 배낭 31개 이상 담긴 장비 → 전체 배낭 조회(청크) 정상
- [ ] 이미지 없는 장비 → 이미지 칸·플레이스홀더가 없는 텍스트 우선 레이아웃 (GD-1). **공유 갤러리 진입점은 여전히 없음** (GD-4 폐기 유지)
- [ ] 보유 장비 → `수정`의 `사진` 필드에서 앨범·카메라 양쪽으로 업로드 성공, 돌아온 상세·창고 목록·배낭 상세 행에도 반영 (GD-13)
- [ ] 미보유(카탈로그) 장비 → 업로드 UI 미노출
- [ ] 사진 교체·삭제 → 화면 즉시 반영, 이전 Storage 파일도 삭제됨
- [ ] **사진 올린 장비를 수정(이름·무게 등) 후 다시 보기 → 사진이 유지된다** (`setDoc` 덮어쓰기로 지워지지 않음)
- [ ] 사진 권한 거부 → 안내 알럿, 에러 문구 없음 / 업로드 실패 → 토스트, 기존 상태 유지
- [ ] 2026-07 이전에 올렸던 사진이 있는 장비 → 기능 복원 후 다시 보인다
- [ ] 공유 배낭 링크를 **다른 계정·비로그인 브라우저**로 열기 → 장비 사진이 보이지 않는다 (§1 비공개 원칙)
- [ ] `coupangUrl`이 있는 카탈로그 장비 → 제품 정보~리뷰 사이 섹션에 섹션 라벨·`쿠팡에서 최저가 보기` 텍스트 링크·수수료 고지 노출, 탭 시 외부 링크
- [ ] `productUrl`만 있는 장비(대다수) → `{브랜드} 공식몰에서 보기` 행만 노출, 파트너스 고지 미노출 (GD-5)
- [ ] 브랜드 행 탭 → **인앱 브라우저 시트**로 열리고, 닫으면 장비 상세로 복귀 (GD-5)
- [ ] 쿠팡 행 탭 → 쿠팡 앱(미설치 시 외부 브라우저)으로 이동 — 인앱 브라우저로 열리지 않는다 (GD-5)
- [ ] 둘 다 있는 장비 → 브랜드 행, 그 아래 쿠팡 행 + 고지 (GD-5)
- [ ] 둘 다 없는 장비·커스텀 장비 → 섹션 전체(섹션 라벨 포함) 미노출 (GD-5)
- [ ] 보유 장비 → 섹션 순서가 정보 → 배낭 기록 → 외부 링크 → 리뷰 (GD-5)
- [ ] 미보유 장비 → 섹션 순서가 정보 → 외부 링크 → 리뷰 (GD-5, 현행 유지)
- [ ] 카테고리 있는 장비 → 무게 줄 아래 태그 칩으로 `카테고리`·`색상`·`사이즈` 표시, 모두 없으면 칩 줄 미노출 (GD-1)
- [ ] 수정하기가 헤더 우상단에 노출 (GD-1)
- [ ] 스크롤을 정보 섹션 아래로 내리면 헤더에 제품명 표시, 최상단 복귀 시 사라짐 (GD-1)
- [ ] USED/USELESS 0회 카드가 시각적으로 낮게 표시 (GD-2)
- [ ] 뒤로가기 VoiceOver 라벨 읽힘 + 버튼 주변 오탭 없이 44pt 터치 (GD-1)

## 8. 미해결 질문

- 컴포넌트 디렉토리 이름이 `warehouse-detail`이지만 라우트는 `gear-detail` — 명칭 불일치(리팩토링 후보).
- ~~`WarehouseDetailSkeletonView`는 정의만 있고 사용되지 않는 데드 코드 — 로딩 스켈레톤을 살릴지 삭제할지 결정 필요.~~ **2026-08-11 해소: 살렸다.** Liquid Depth 골격으로 다시 그리고 래퍼의 초기화 전 분기(`null`)를 이 스켈레톤으로 바꿨다 — 이 앱은 로딩에 스피너를 쓰지 않으므로(핸드오프) 빈 지면을 남길 자리가 없다(§6 로딩).
- 외부 후기 콘텐츠(`components/review/ReviewSectionView`)와 별점(`camp-site/StarRatingView`)은 **박지 상세와 공유**하는 모듈이라 아직 ACG 토큰을 쓴다 — 박지 상세를 이식할 때 함께 옮긴다. 장비 상세에서는 섹션 껍데기만 Liquid이고 그 안의 후기 행·영상 카드는 이전 세대 값이다(흰 면·회색 글자라 톤 차이는 작다).
