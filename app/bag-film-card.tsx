import BagFilmCardWrapper from '@/components/bag-film-card/BagFilmCardWrapper';

// BS-1: 배낭 상세 헤더의 `필름 카드` 아이콘으로만 진입하는 이미지 카드 화면.
// 대상 배낭(BagDetail)은 BagFilmCardHandoff로 전달받는다.
const BagFilmCardScreen = () => {
  return <BagFilmCardWrapper />;
};

export default BagFilmCardScreen;
