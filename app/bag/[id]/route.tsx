import BagRouteWrapper from '@/components/bag-route/BagRouteWrapper';

// 배낭 코스(GPX) 화면 — 지도·목록·고도 그래프 (BD-11).
const BagRouteScreen = () => {
  return <BagRouteWrapper />;
};

export default BagRouteScreen;
