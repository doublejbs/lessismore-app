/**
 * 필름 카드 화면에서 **프리뷰 위에 떠 있는 컨트롤** 전용 팔레트(BS-10).
 *
 * **디자인 토큰을 쓰지 않는 이유**: 이 컨트롤들은 앱 UI지만 카드 프리뷰(사진) 위에 뜬다.
 * 토큰의 밝은 칩(흰 배경 + `chipBorder` 아웃라인)을 그대로 쓰면 밝은 사진 위에서 묻히고,
 * 반대로 반투명 검정만 쓰면 프리뷰 위아래에 남는 **검정 레터박스** 위에서 경계가 사라진다.
 * 그래서 이 화면에서만 `반투명 검정 채움 + 흰 글자·아이콘 + 옅은 흰 테두리` 조합을 직접 쓴다.
 * `CLAUDE.md` 토큰 규칙의 의도적 예외이며, 다른 화면으로 가져가지 않는다.
 */
export const FilmCardControlPalette = {
  // 반투명 검정. 흰 사진 위에 얹혀도 흰 글자 대비가 4.5:1을 넘도록 55%로 잡았다.
  background: 'rgba(0, 0, 0, 0.55)',
  // 검정 레터박스 위에서 컨트롤 경계가 사라지지 않게 두르는 아주 옅은 흰 테두리.
  border: 'rgba(255, 255, 255, 0.28)',
  foreground: '#FFFFFF',
  // 선택된 칩과 주 액션(공유하기)은 흰색으로 채우고 글자를 검정으로 뒤집는다.
  selectedBackground: '#FFFFFF',
  selectedForeground: '#000000',
} as const;
