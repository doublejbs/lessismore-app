// 검색 필드의 두 세대. `Glass`(기본)는 ACG 유리 면이고, `Plain`은 레퍼런스 탐색 화면의
// 연회색 알약(테두리·유리·그림자 없음)이다. 순백 지면 화면·시트는 `Plain`을 쓴다.
enum SearchBarVariant {
  Glass = 'glass',
  Plain = 'plain',
}

export default SearchBarVariant;
