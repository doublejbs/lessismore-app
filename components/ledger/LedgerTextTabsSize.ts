/**
 * `LedgerTextTabs` 항목의 글자 급.
 *
 * 두 단뿐이다 — 1차 줄(`Md`)과 그 아래 딸린 세분 줄(`Sm`). 세분 줄이 한 급 작아야 1차
 * 선택에 딸린 보조 줄임이 크기만으로 읽힌다.
 */
enum LedgerTextTabsSize {
  Md = 'md',
  Sm = 'sm',
}

export default LedgerTextTabsSize;
