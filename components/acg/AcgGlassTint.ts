/**
 * 유리 면의 채움 계열(`AcgGlassView`).
 *
 * 리퀴드 글래스는 **재질이지 색이 아니다** — 같은 유리라도 밝은 배경 위에서 떠 보이려면
 * 밝게, 화면에서 가장 무거운 면(주 액션·토스트)으로 읽히려면 어둡게 깔아야 한다.
 */
enum AcgGlassTint {
  // 밝은 유리. 검색 필드, 보조 버튼, 하단 바, 지도 오버레이 등 대부분의 크롬.
  Clear = 'clear',
  // 잉크 유리. 유리이면서도 주 액션·토스트로 읽혀야 하는 면.
  Ink = 'ink',
}

export default AcgGlassTint;
