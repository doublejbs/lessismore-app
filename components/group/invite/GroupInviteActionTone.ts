/**
 * 초대 복사 버튼이 놓이는 바탕 (GRP-3 · GRP-11).
 * 흰 지면 위(하단 초대 섹션)에는 연회색 채움, 연회색 카드 안(혼자일 때 초대 카드)에는 흰 채움이다 —
 * 카드와 같은 연회색이면 버튼 면이 카드에 묻힌다.
 */
enum GroupInviteActionTone {
  OnPaper = 'OnPaper',
  OnSurface = 'OnSurface',
}

export default GroupInviteActionTone;
